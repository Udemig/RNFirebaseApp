import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SpecialIcon = () => {
  return (
    <View>
      <Text>SpecialIcon</Text>
    </View>
  )
}

export default SpecialIcon

const styles = StyleSheet.create({})